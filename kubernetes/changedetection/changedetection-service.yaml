apiVersion: v1
kind: Service
metadata:
  name: changedetection
  namespace: changedetection
spec:
  selector:
    app: changedetection
  ports:
  - port: 5000
    targetPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: playwright-chrome
  namespace: changedetection
spec:
  selector:
    app: playwright-chrome
  ports:
  - port: 3000
    targetPort: 3000