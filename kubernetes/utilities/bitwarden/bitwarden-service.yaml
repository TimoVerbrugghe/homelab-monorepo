apiVersion: v1
kind: Service
metadata:
  name: bitwarden
  namespace: utilities
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app.kubernetes.io/name: bitwarden