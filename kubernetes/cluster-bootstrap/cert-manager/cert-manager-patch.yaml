# Add autoupdate through keel for cert-manager
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cert-manager
  annotations:
    keel.sh/policy: all
    keel.sh/trigger: poll
    keel.sh/pollSchedule: "@every 24h"