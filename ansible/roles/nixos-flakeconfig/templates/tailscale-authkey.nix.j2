{ lib, ... }:

with lib;

{
  options = {
    tailscaleAuthKey = mkOption {
      type = types.str;
      default = "";
      description = "Tailscale authentication key";
    };
  };

  config = {
    tailscaleAuthKey = "{{ tailscaleAuthkey }}"; # Place tailscale Auth Key here
  };
}