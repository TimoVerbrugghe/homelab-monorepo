# this is the user that has ssh access
ansible_user: root

# Github Username for SSH Keys
github_username: TimoVerbrugghe

# Proxmox connectivity
proxmox_api_user: root@pam
proxmox_api_tokenid: ansible
proxmox_api_tokensecret: 
proxmox_api_host: routerpve.timo.local
proxmox_node: routerpve

# Cloud Init settings (username and password)
template_ciuser: admin
template_cipassword: admin

ubuntu_cloud_image_url: https://cloud-images.ubuntu.com/releases/22.10/release/ubuntu-22.10-server-cloudimg-amd64.img
template_id: 9999
template_name: ubuntu-cloud
bridge: vmbr1
template_storage: local-zfs