- name: clean docker
  hosts: all
  tasks:
    - name: prune non-dangling images & cache
      community.docker.docker_prune:
        containers: false
        images: true
        images_filters:
          dangling: false
        networks: false
        volumes: false
        builder_cache: true