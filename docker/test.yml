version: "3.8"

services: 
  whoami:
    image: "traefik/whoami"
    container_name: "simple-service"
    networks:
      - bridge
      - dockerproxy

networks:
  dockerproxy:
    external: true
  
  bridge:
    external: true